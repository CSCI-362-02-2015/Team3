//comments
module “credential“ do
	describe "credential_match" do
    	it "compares the protocol" do
			credentialDriver.c credential.h 
			https, example.com, foo.git, bob, http, example.com, foo.git, bob
			expected 0
	    end
	end
end